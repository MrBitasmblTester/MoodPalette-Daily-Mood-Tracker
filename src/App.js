import React, {useState, useEffect} from \"react\";\nimport MoodSelector from \"./components/MoodSelector\";\nimport HeatMap from \"./components/HeatMap\";\nconst STORAGE_KEY = \"moodData\";\nfunction App() {\n  const [moods, setMoods] = useState({});\n  useEffect(() => {const saved=localStorage.getItem(STORAGE_KEY); if(saved) setMoods(JSON.parse(saved));}, []);\n  useEffect(() => {localStorage.setItem(STORAGE_KEY, JSON.stringify(moods));}, [moods]);\n  const handleSelect = (date, mood) => setMoods(prev=>({...prev, [date]: mood}));\n  const today = new Date().toISOString().slice(0,10);\n  return (\n    <div>\n      <MoodSelector currentDate={today} onSelect={handleSelect}/>\n      <HeatMap moods={moods}/>\n    </div>\n  );\n}\nexport default App;